# Logs
logs
receiver_logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

dist-ui
dist-electron
dist-server
dist-release

.env
bootstrap-peer-id*.bin
bootstrap-datastore
relay-peer-id.bin

# Server runtime metadata (public, regenerated each start)
*.runtime.json

# Local infra deployment data (identities, datastores, runtime output, secrets).
# Per-mode instances live under infrastructure/instances/<slug>/ and hold .env,
# peer-identity keys, datastores, Tor onion keys, and the TURN credential.
infrastructure/instances
infrastructure/.env
infrastructure/data
infrastructure/run
infrastructure/secrets

.claude
.codex

kiyeovo-downloads
kiyeovo-uploads